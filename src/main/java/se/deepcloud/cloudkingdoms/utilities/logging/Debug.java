package se.deepcloud.cloudkingdoms.utilities.logging;

import java.util.Locale;

public enum Debug {

    CREATE_KINGDOM,
    DELETE_KINGDOM,
    SORT_KINGDOMS,
    CALCULATE_ALL_KINGDOMS,
    PURGE_KINGDOM,
    UNPURGE_KINGDOM,
    REGISTER_SORTING_TYPE,
    SET_LAST_KINGDOM,
    SET_BLOCK_AMOUNT,
    GET_WORTH,
    GET_LEVEL,
    SAVE_SCHEMATIC,
    REWARD_MISSION,
    EXECUTE_COMMAND,
    INVITE_MEMBER,
    REVOKE_INVITE,
    ADD_MEMBER,
    KICK_MEMBER,
    BAN_PLAYER,
    UNBAN_PLAYER,
    ADD_COOP,
    REMOVE_COOP,
    SET_COOP_LIMIT,
    ENTER_KINGDOM,
    LEAVE_KINGDOM,
    SET_KINGDOM_HOME,
    SET_VISITOR_HOME,
    SET_PERMISSION,
    RESET_PERMISSIONS,
    SET_NAME,
    SET_DESCRIPTION,
    SET_MEMBER_BONUS,
    SET_CLAIMS_BONUS,
    TRANSFER_KINGDOM,
    SET_IGNORED,
    SEND_MESSAGE,
    SEND_TITLE,
    SET_LOCKED,
    EXECUTE_KINGDOM_COMMANDS,
    SET_KINGDOM_LAST_TIME_UPDATED,
    SET_BANK_LIMIT,
    GIVE_BANK_INTEREST,
    SET_BONUS_WORTH,
    SET_BONUS_LEVEL,
    SET_UPGRADE,
    SET_CROP_GROWTH,
    SET_SPAWNER_RATES,
    SET_MOB_DROPS,
    CLEAR_BLOCK_LIMITS,
    SET_BLOCK_LIMIT,
    REMOVE_BLOCK_LIMIT,
    CLEAR_ENTITY_LIMITS,
    SET_ENTITY_LIMIT,
    SET_TEAM_LIMIT,
    SET_WARPS_LIMIT,
    SET_KINGDOM_EFFECT,
    REMOVE_KINGDOM_EFFECT,
    CLEAR_KINGDOM_EFFECTS,
    SET_ROLE_LIMIT,
    REMOVE_ROLE_LIMIT,
    CREATE_WARP_CATEGORY,
    DELETE_WARP_CATEGORY,
    CREATE_WARP,
    DELETE_WARP,
    REMOVE_RATING,
    REMOVE_RATINGS,
    ENABLE_KINGDOM_FLAG,
    DISABLE_KINGDOM_FLAG,
    SET_GENERATOR_PERCENTAGE,
    SET_GENERATOR_RATE,
    REMOVE_GENERATOR_RATE,
    CLEAR_GENERATOR_RATES,
    GENERATE_BLOCK,
    SET_KINGDOM_MISSION_COMPLETED,
    DATABASE_QUERY,
    MENU_CLICK,
    OPEN_MENU,
    BLOCK_PLACE,
    BLOCK_BREAK,
    BLOCK_COUNT_INCREASE,
    BLOCK_COUNT_DECREASE,
    CHUNK_CALCULATION,
    ENTITY_SPAWN,
    ENTITY_DESPAWN,
    DEPOSIT_MONEY,
    WITHDRAW_MONEY,
    SET_TEXTURE_VALUE,
    SET_PLAYER_LAST_TIME_UPDATED,
    SET_PLAYER_KINGDOM,
    SET_PLAYER_ROLE,
    SET_DISBANDS,
    SET_ADMIN_BYPASS,
    SET_TOGGLED_PANEL,
    SET_KINGDOM_FLY,
    SET_LEAVING_FLAG,
    SET_WARP_NAME,
    SET_WARP_LOCATION,
    SET_WARP_PRIVATE,
    SET_WARP_ICON,
    SET_WARP_CATEGORY_NAME,
    SET_WARP_CATEGORY_ICON,
    SET_WARP_CATEGORY_SLOT,
    TELEPORT_PLAYER,
    LOAD_CHUNK;

    private static String[] DEBUG_NAMES = null;

    public static String[] getDebugNames() {
        if (DEBUG_NAMES == null) {
            Debug[] debugs = Debug.values();
            DEBUG_NAMES = new String[debugs.length];
            for (int i = 0; i < debugs.length; ++i)
                DEBUG_NAMES[i] = debugs[i].name().toLowerCase(Locale.ENGLISH);
        }

        return DEBUG_NAMES;
    }
}