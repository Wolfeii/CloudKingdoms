package se.deepcloud.cloudkingdoms.command.admin;

import org.bukkit.command.Command;
import org.jetbrains.annotations.NotNull;
import se.deepcloud.cloudkingdoms.CloudKingdoms;
import se.deepcloud.cloudkingdoms.command.CommandMap;

public class AdminCommandMap extends CommandMap {

    public AdminCommandMap(@NotNull CloudKingdoms plugin) {
        super(plugin);
    }

    @Override
    public void loadDefaultCommands() {

    }
}
